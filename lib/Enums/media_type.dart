import 'package:owomaniya/Enums/enum_values.dart';

enum MediaType { cover_image, video, image }

final mediaTypeValues = EnumValues({
  "cover_image": MediaType.cover_image,
  "image": MediaType.image,
  "video": MediaType.video,
});
